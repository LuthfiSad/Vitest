{
  "name": "belajar-vitest",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest",
    "global": "vitest --run tests/global.test.ts",
    "multiple": "vitest --run tests/slow.test.ts",
    "chai": "vitest --run tests/chai.test.ts",
    "mocking": "vitest --run tests/mocking.test.ts",
    "bench": "vitest bench --run tests/hello.bench.ts",
    "coverage": "vitest --coverage --run tests/say-hello.test.ts",
    "ui": "vitest --ui"
  },
  "devDependencies": {
    "@vitest/ui": "^1.4.0",
    "vite": "^5.2.0"
  },
  "dependencies": {
    "@vitest/coverage-istanbul": "^1.4.0",
    "typescript": "^5.4.4",
    "vitest": "^1.4.0"
  }
}
